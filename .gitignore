apiguard-assurance.zip

# Python bytecode
__pycache__/
*.py[cod]
*$py.class
*.pyc

# Virtual environments
.venv/
venv/
env/
ENV/

# Distribution / packaging
dist/
build/
*.egg-info/
*.egg
MANIFEST

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
nosetests.xml

# Tool outputs — generated at runtime, never committed.
# The outputs/ directory is the canonical location for assessment results.
# Individual filenames are kept as fallbacks for compatibility with any
# existing CI scripts that still reference them directly.
outputs/
evidence.json
assessment_report.html
test_report.html
*.log

# Secrets and local config overrides
config.local.yaml
.env
.env.*
!.env.example

# IDE and editor artifacts
.idea/
.vscode/*
!.vscode/settings.json
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Ruff cache
.ruff_cache/

# Docker (compose override files with local secrets)
docker-compose.override.yml
docker-compose.local.yml

# Apiguard 
specs/